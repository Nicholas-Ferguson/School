---

- hosts: sw1
  gather_facts: no
  tasks:
    - name: Configure sw1 Hostname
      cisco.ios.ios_system:
        hostname: sw1

- hosts: sw2
  gather_facts: no
  tasks:
    - name: Configure sw2 Hostname
      cisco.ios.ios_system:
        hostname: sw2
